{
  "query": [
    {
      "operator": "getBE",
      "id": "{{params.idField}}",
      "as": "{{params.asField}}"
    },
    {
      "operator": "populateAttributes",
      "code": "{{params.idField}}",
      "as": "attributes"
    }
  ]
}