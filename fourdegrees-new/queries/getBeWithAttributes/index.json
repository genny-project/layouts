{
  "query": [
    {
      "operator": "getBE",
      "id": "{{params.idField}}",
      "as": "{{params.asField}}"
    },
    {
      "operator": "scope",
      "path": "{{params.asField}}",
      "scope": {
        "operator": "populateBEGAttributes",
        "as": "attributes"
      }
    }
  ]
}